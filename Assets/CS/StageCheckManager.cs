using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//***************************************
// ステージ参照用スクリプト
//***************************************
public class StageCheckManager : MonoBehaviour
{
    //*************************
    // 使用メンバ変数
    //*************************
    public static int StageCount = 0;  // ステージ数を持つクラス

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
